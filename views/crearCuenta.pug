doctype html
html(lang="en")
    head
        include partial/header
    body.login 
        .contenedor-formulario
            h1 Crea tu cuenta en Uptaks
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
              
            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo 
                    label(for="email") Email:
                    input(type="text", name="email" placeholder="Ingresa tu email" value= email)
                .campo 
                    label(for="password") Contraseña:
                    input(type="password", name="password" placeholder="Ingresa tu contraseña" value= password)
                .campo.enviar 
                    input.boton(type="submit" value="Crear cuenta")
                .campo 
                    a(href="/iniciar-session") Volver a iniciar session 
        script(src="/dist/bundle.js") 