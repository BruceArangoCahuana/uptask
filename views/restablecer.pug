doctype html
html(lang="en")
    head
        include partial/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            p Agreaga tu E-mail para restablecer contraseña
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
            form.caja-login(action="/restablecer", method="post")  
                .campo 
                    label(for="email") Email:
                    input(type="text", name="email" placeholder="Ingresa tu email" value= email)
               
                .campo.enviar 
                    input.boton(type="submit" value="Restablecer")
                .campo.acciones 
                    a(href="/iniciar-session") Ya recorde iniciar session 